"""
Use cases — orchestration logic.

Step 2 ships skeletons for the use cases that other steps wire up.
Step 3 implements `submit_code` + `record_verdict` against the walking-skeleton
adapter. Step 4 implements `import_problem`.
"""
